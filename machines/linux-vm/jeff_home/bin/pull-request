if [ -z "$1" ]
then
   echo "usage pull-request branch"
   exit 0
fi

branch=origin/$1

get fetch origin 

base=`git merge-base origin/develop $branch`

git diff --name-only $base $branch
read -p "Launch diff y/n: " yesno
if [ "$yesno" = "y" ]
then
    git difftool -y $base $branch
fi
